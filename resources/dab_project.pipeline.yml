# The main pipeline for dab_project
resources:
  pipelines:
    git_cicd_test_pipeline:
      name: git_cicd_test_pipeline
      catalog: workspace
      schema: dab_project_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
