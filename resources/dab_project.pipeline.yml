# The main pipeline for dab_project
resources:
  pipelines:
    bia_DLT_pipeline_CICD_test_final:
      name: bia_DLT_pipeline_CICD_test_final
      catalog: workspace
      schema: dab_project_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
